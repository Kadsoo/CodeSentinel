# 反思报告初版草稿


## 项目与方法

我选择 Coding Agent Harness 方向，构建了 CodeSentinel。项目没有把现成 Agent Runner 当成核心，而是自己实现了 Action 解析、Policy Guardrail、工具分发、测试反馈、人工审批和停止机制。这个选择最初看起来比直接调用一个 Agent 框架更慢，但它让我必须把“模型想做什么”和“系统允许做什么”分开。最终的主贡献不是让模型更会写代码，而是让每一个危险动作都经过可以单测的边界。

Superpowers 中对我作用最大的是 brainstorming、writing-plans、test-driven-development、subagent-driven-development 和 requesting-code-review。brainstorming 帮我把“做一个编程 Agent”拆成工作区、Provider、治理、反馈、记忆和分发等不同问题；writing-plans 把大任务改成可以在一个 worktree 中完成的小任务；TDD 则迫使我在实现前先写出失败测试。形式最容易大于实质的部分是机械地记录流程名称。如果只把技能名写进日志，却没有记录为什么改变设计、哪个测试先失败，那么这些文件只是一种项目装饰。真正有价值的是把失败、修复和重新验证连起来。

## TDD 与 AI 协作

在 AI 协作中，TDD 对我既是阻碍也是放大器。阻碍在于，写测试前要先想清楚接口、边界和错误契约，不能直接让模型开始堆代码；放大器在于，一旦测试写清楚，模型可以快速尝试实现，而我可以用测试结果判断它是否偏离了要求。这个项目里最有用的 RED 不是“函数不存在”这种浅层失败，而是路径越界、基线哈希变化、审批过期、脱敏后的 SQLite 字节仍泄露等边界失败。它们把安全要求变成了可以反复运行的证据。

我发现 task 颗粒度以一个明确边界为宜：一个 task 最好只负责一个接口或一个状态转移，并且能用一个 focused test 命令验证。Task 太大时，subagent 会同时修改多个层次，审查很难判断根因；Task 太小时，流程记录和上下文切换会变成负担。三到五分钟可以描述清楚、半小时到一小时可以完成并验证的任务，比较适合作为一个 worktree 的最小单位。

## 规约、上下文与偏离

SPEC 和 PLAN 的质量直接影响实现质量。早期计划只描述“支持 Agent 运行测试”，没有明确验证命令如何绑定到会话，结果容易把任意字符串当成可执行命令。后续我把 launcher、参数数组、超时、输出上限和命令 ID 写成了显式约束，Policy 才能在代码层拒绝未知命令。另一个教训是，安全要求不能只写“不要泄露 Key”，必须指出 Key 不进入哪些具体边界：源代码、Git 历史、日志、SQLite、终端、HTTP 响应和公开演示。

最有效的 prompt/context 策略不是一次性塞入整个仓库，而是每次给 subagent 一个明确任务、准确文件路径、预期 RED 结果和验证命令。遇到不确定性要求它暂停提问，比让它猜测更省时间。独立的冷启动 agent 暴露了计划里没有写清的假设，这种反馈比继续补充口头解释更有价值。后续我会把“为什么不做某个功能”也写进上下文，避免 agent 为了看起来完整而扩展范围。

## 凭据、分发与公开演示

凭据要求迫使我承认“能调用 API”不等于“能安全交付”。CLI 采用隐藏输入和 Windows Credential Manager，状态接口只返回 configured/missing，Provider 不能把 Key 写进 Profile 或日志。分发要求又暴露了另一个问题：本地 Agent 需要工作区和操作系统能力，不能直接做成公开服务让陌生人提交代码执行。因此公开 WebUI 采用只读 Mock 事件，线上展示治理和反馈机制，但不接收路径、代码或 Key；真正的 Agent 仍在用户本机运行。这种拆分牺牲了一些“在线体验”，却避免把任意代码执行能力暴露到公网。

## 局限与重做计划

当前实现仍有明显边界。Windows 原生凭据、SQLite 原生模块和干净机器安装还需要更多平台冒烟测试；真实 Provider 的 endpoint、模型和网络错误也不能只靠 Mock 推断。公开 Mock 页面证明了机制可展示，但还不能证明真实用户完成一次端到端修复。若重新开始，我会更早建立最小 WebUI 和分发路径，而不是把全部时间集中在后端安全细节之后再补材料；我也会在第一天明确课程要求的 CI 平台、NJU Git 提交流程和线上 URL 责任人。

我对 Superpowers 的批判是：它假设项目所有者愿意持续维护计划、日志和审查纪律，也假设 task 可以被稳定切分。现实中，文档本身也会产生冲突，流程成本会在小任务上显得很高。但在 AI 能快速生成大量代码的情况下，这些成本是必要的摩擦。它不能替我判断产品是否值得做，也不能替我写出可信的反思；它真正提供的是一组让判断留下证据的约束。我最终学到的不是“让 AI 自动完成项目”，而是如何把 AI 的速度放进一个可验证、可停止、可解释的工程闭环。
